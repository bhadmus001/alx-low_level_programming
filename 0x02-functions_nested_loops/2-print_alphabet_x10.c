#include "main.h"
/**
 * print_alphabet_10x - Print the english alphabet 10 times
 */
void print_alphabet_10x(void)
{
	int u = 0;
	char v;

	while (u <= 9)
	{
		v = 'a';
		while (v <= 'z')
		{
			_putchar(v);
			v++;
		}
	a++;
	}
}
